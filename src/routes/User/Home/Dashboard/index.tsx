import Header from "@/components/custom/Header";
import { memo } from "react";

const UserDashboard = () => {
    return (
        <>
            <Header />
        </>
    );
};

export default memo(UserDashboard);
